function monitorCount(rows,columns){
  return rows*columns;
}
const numOfMonitors=monitorCount(5,4);
console.log(numOfMonitors);
/* This lesson taught about the return keyword. Return is used to make a function produce data as an output.
This will be helpful for data manipulation as we can have a function do all of the repetive work and simply return the final result to us.
*/
