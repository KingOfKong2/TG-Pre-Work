let isLocked = false;

isLocked ? console.log('You will need a key to open the door.') : console.log('You will not need a key to open the door.');

let isCorrect = true;

isCorrect ? console.log('Correct!') : console.log('Incorrect!');

let favoritePhrase = 'Love That!';

favoritePhrase === 'Love That!' ? console.log('I love that!') : console.log("I don't love that!");
/* THis lesson taught about the ternary operator. The ternary operator is a simple way of creating an if...else statement in one line.
The condition to be checked is followed by a ?, then the true statement is followed by a colon and the else statement is followed with a semicolon.
Example:
1 ? console.log('True!') : console.log('False!');
replaces
if(1)
{
	console.log('True!');
}
else{
	console.log('False!');
}
*/
