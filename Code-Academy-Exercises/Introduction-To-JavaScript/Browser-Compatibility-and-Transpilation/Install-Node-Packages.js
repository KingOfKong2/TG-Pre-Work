/*
We used the npm install command to install new Node packages.
We append the -D  flag to add the package to our dependencies.
This will make it easier to share our code because when developers install our package it will know which dependencies it needs.
*/
