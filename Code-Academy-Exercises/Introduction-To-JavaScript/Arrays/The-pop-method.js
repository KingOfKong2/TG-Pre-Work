const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];
chores.pop();
console.log(chores);
/* We learned that we can remove the last element from the array with the .pop() command.
The item that is popped can be saved for a variable and can be useful for dealing with a list of items.
*/
