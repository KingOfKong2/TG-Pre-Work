let myName='Andy';
let myCity='Vegas';
console.log(`My name is ${myName}. My favorite city is ${myCity}`);
/* This lesson taught about string interpolation. If we define a string with back ticks (`) we can include variable in the string without having to concatonate.
To specify which string we would like to use we must use the keyword ${VARNAME}.
*/
