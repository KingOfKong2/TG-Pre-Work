let newVariable = 'Playing around with typeof.';
console.log(typeof newVariable);
newVariable=1;
console.log(typeof newVariable);
/* This lesson taught about using the typeof operator. typeof will return the type of the operator passed to it as a string.
This is useful because we may not be sure what type our variable is if we change its value over time.
*/
