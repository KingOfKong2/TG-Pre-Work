class Surgeon{
  constructor(name, department){
    this.name=name;
    this.department=department;
  }
}
/*
This lesson taught the syntax of creating a class. Each class must have a constructor (as it is called each time a new instance of that class is made).
When assigning local variable we must also use the this keyword to ensure our data is stored correctly.
*/
